function [t, y] = Euler_implicito(f, a, b, ya, N, tol, max_iter)
    h = (b - a)/N;
    t = a:h:b;
    t = t(:);
    y = zeros(N+1, 1);
    y(1) = ya;
    
    for k = 1:N
        x0 = y(k);
        iter = 1;
        while (iter < max_iter && dif > tol)
        
        end
    end
end
